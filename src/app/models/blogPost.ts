export interface BlogPost {
    title: string,
    content: string,
    releaseDate: string,
    likesCount: number,
    commentsCount: number,
    authorName: string,
    authorId:number
    // cardImageUrl:string,
    // authorImageUrl:string,
    // categoryName:string
}